fun main() {
    var x : Float = 13F
    var y = 3F
    println(x / y)
    println(y / x)

    // Home Work
    val pi = 3.14
    var r = 5.0
    var area = pi*r*r
    var volume = 0.75*pi*r*r*r
    println("The Circumference of the Circle of radius $r is ${2*pi*r}")
    println("The Area of the Circle of radius $r is $area")
    println("The Volume of the Sphere of radius $r is $volume")

}